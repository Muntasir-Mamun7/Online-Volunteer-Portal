# Online-Volunteer-Portal

## Admin saving
The “Save to GitHub” button uses a GitHub Personal Access Token stored in your browser. Set the token in localStorage (or sessionStorage for a single-tab session) under the key `gh_admin_token` before saving. If the token is missing or invalid, saving will be blocked.
